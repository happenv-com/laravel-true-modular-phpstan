<?php

declare(strict_types=1);

/*
 * Each test file binds the specific PHPStan test case it needs via uses(...),
 * so there is no global base test case. This file exists for Pest's bootstrap
 * and shared helpers only.
 */
